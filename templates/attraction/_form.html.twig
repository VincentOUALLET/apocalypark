{{ form_row(form.attraction_image) }}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mb-5">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
